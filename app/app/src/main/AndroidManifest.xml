<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jaisa.smarttraumareliever_flawsome">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission-sdk-23 android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" >

        </activity>
        <activity android:name=".ProfileActivity">
        </activity>
        <activity android:name=".ReportCrimeActivity" />
        <activity
            android:name=".DisplayCrimesActivity"
            android:label="@string/title_activity_display_crimes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CrimeDetailsActivity">

        </activity>
        <activity android:name=".FIRReportActivity"></activity>
        <activity android:name=".SolvedCrimesActivity"/>
        <activity android:name=".NoResultsActivity"/>
    </application>

</manifest>